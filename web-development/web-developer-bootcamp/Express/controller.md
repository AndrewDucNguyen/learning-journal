# Controller
- Controllers are used within routes to pull out the logic into a separate file and then implement it with the function that calls the logic
- Controllers sends objects
- Comes from MVC
    - Model: Data heavy stuff with models
    - Views: All view contnet, layout, what users sees
    - Controllers: Heart of application, main logic happens, rendering views and working with models